name: Test Lighthouse Check
on: [push]

jobs:
  audit:
    runs-on: foosoftware/lighthouse-check:latest
    steps:
    - uses: actions/checkout@master
    - name: Run Lighthouse
      uses: ./
      with:
        urls: 'https://www.foo.software,https://www.foo.software/contact'
