"use strict";

var _getOpportunities = require("./getOpportunities");

describe('Util', () => {
  it('Util.calculateRating should be a function', () => {
    expect(typeof _getOpportunities.Util.calculateRating).toBe('function');
  });
  it('Util.showAsPassed should be a function', () => {
    expect(typeof _getOpportunities.Util.showAsPassed).toBe('function');
  });
});